# dnilabs.com

Small static personal website.

The blog posts were migrated from the old [`blog`](https://github.com/dni/blog) repository into this site as plain static HTML. The generated post overview uses `blogs.js`, and the individual posts live in `blogs/`.

The old blog repository is now archived.
